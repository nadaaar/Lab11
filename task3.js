function power(a,b){
    return Math.pow(a,b);

}

function rounding(a){

    return Math.round(a);
}

function ceiling(a){

    return Math.ceil(a);
}

module.exports.power=power;
module.exports.rounding=rounding;
module.exports.ceiling=ceiling;